# .env is better suited for public variables, ie, variables that should not commited
# For secret variables is better to use DevServerControl tool with set_env_variable: ["KEY", "SECRET"]

PING_MESSAGE="ping pong"

# Octagon API Configuration
# In development: Uses local server automatically (runs on same port as dev server)
# In production: Set this to your deployed API URL
# VITE_OCTAGON_API_URL=https://your-api-domain.com
#
# Force offline mode for testing:
# VITE_USE_OFFLINE_MODE=false
